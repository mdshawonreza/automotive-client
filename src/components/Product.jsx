


const Product = ({product}) => {
     const {_id,photo,brandName}=product

     
    return (
        <div>
           <h2>title{_id}</h2>
            
        </div>
    );
};

export default Product;